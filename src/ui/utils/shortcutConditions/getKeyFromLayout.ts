// async function getKeyFromLayout(key: string): string | null {
//   if (navigator.keyboard!) {
//     const keyboard = navigator.keyboard
//
//     const layout: Map<string, string> = await keyboard.getLayoutMap()
//
//     return Layout.get('KeyW') ?? null
//   }
// }
//
